# Rules
1. No Human Intervantion will be provided, as the human is going to sleep
2. Research About the Feature, Locally and online ( if required )
3. Start Crating a Plan, run opus 4.7 to check that plan
4. Start crating tasks, then start coding, and test it live on adb via view tree, not random clicks on screen from screenshots
5. Once Done, Fixing and testing, Move to Next Feature & Repeat
6. Always Read the Memory, and don't commit anything yet

# Features 

- [ ] VLM support in home screen and in Model Store With a Proper Structure of VLM having their Own FOlders in which the LLM + MMPROJ will live and at the time of download from any repo of VLM when the user downloads the LLM it will download the MMPROJ also and if already exist then we will skip that file and every llm model and mmproj downloaded from the VLM rrepo will be saved in their named folder, this will when user loads a VLM ( LLM model ) it will auto load the mmproj so user don't have to manully load it
- this also requires to remove all the mmproj load button 

# Local project Folders
- /home/home/AndroidStudioProjects/ToolNeuron-New
- /home/home/AndroidStudioProjects/ToolNeuron
- /home/home/AndroidStudioProjects/AiSystems